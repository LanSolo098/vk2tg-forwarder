# ===== VK to Telegram Forwarder Configuration =====
# Скопируй этот файл в .env и заполни значения.

# VK user access token со scope 'messages' и 'offline'.
VK_TOKEN=vk1.a.сюда_твой_токен

# Целевая беседа ВК (peer_id). Для многопользовательских чатов это 2000000000 + chat_id.
# Узнаем ниже через python get_chats.py.
VK_CHAT_PEER_ID=2000000123

# Telegram bot token от @BotFather
TG_BOT_TOKEN=1234567890:ABCDEF-your-bot-token

# Куда слать в Telegram: @username канала или numeric chat_id (-100xxxxxxxxxxxx)
TG_TARGET=@имя_твоего_канала

# Необязательно: фильтры типов. Пусто — пересылать всё.
INCLUDE_TYPES=
EXCLUDE_TYPES=

# Префикс перед каждым сообщением (необязательно)
TG_PREFIX=[VK] 

# Таймауты и логи
HTTP_TIMEOUT=20
DEDUP_CACHE=500
LOG_LEVEL=INFO
